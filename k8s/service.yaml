apiVersion: v1
kind: Service
metadata:
  name: todo-app-service
  namespace: todo-app
  labels:
    app: todo-app
spec:
  selector:
    app: todo-app
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
